{% extends '@bestopheVideoCollection/layout.html.twig' %}

{% block title %}{{ 'addNewMovie.search'|trans([], 'menu') }}{% endblock %}

{% block content %}

    <div class="panel panel-default">
        <div class="panel-body">
            {{ form_start(MovieSearchForm, {'attr': {'class': 'form-inline'}}) }}  

            <div class="form-group">
                {{ form_errors(MovieSearchForm.title) }}
                {{ form_widget(MovieSearchForm.title, { 'attr':  {
                    'rows': '1'
                }}) }}
                {#{{ form_widget(MovieSearchForm.title, { 'attr':  {
                    'rows': '1',
                   'placeholder': 'addNewMovie.placeholder'|trans([], 'menu')
                }}) }}#}
            </div>
            <div class="form-group">
                <input type="submit" class="btn btn-primary" value="Go" />
            </div>
        </div>
    </div>
    {{ form_end(MovieSearchForm) }}
   {# {% for flashMessage in app.session.flashbag.get('success') %}
        <div class="alert alert-success">
            {{ flashMessage }}
        </div>


    {% endfor %}#}
  

{% endblock %}
