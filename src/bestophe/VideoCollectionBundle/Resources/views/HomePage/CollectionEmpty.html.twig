{% extends '@bestopheVideoCollection/layout.html.twig' %}

{% block title %}{{ 'collectionEmpty.tabTitle'|trans([], 'menu') }}{% endblock %}

{% block content %}
    <div class="panel panel-default">
        <div class="panel-heading">
            <h3 class="panel-title">{{ 'collectionEmpty.title'|trans([], 'menu') }}</h3>
        </div>
        <div class="panel-body">
            <p>{{ 'collectionEmpty.toDo'|trans([], 'menu') }} <a href="{{ path('bestophe_VideoCollection_addNewMovie') }}">{{ 'main.add'|trans([], 'menu') }}</a></p>
        </div>
    </div>
{% endblock %}